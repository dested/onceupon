# Any of these is optional; keys can also be pasted into the in-app settings (stored in localStorage).
# These are read in the BROWSER. Localhost only. Never deploy this app with keys in it.
VITE_ANTHROPIC_API_KEY=
VITE_OPENROUTER_API_KEY=
VITE_OPENAI_API_KEY=
